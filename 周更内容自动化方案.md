# 周更内容创作自动化系统 · 实现方案

> 适用账号：**老张讲财税**（B 端老板财税风险科普，本地张老师数字人出片）
> 目标：每周一生成 15 条爆款选题供选 → 每天下午/晚上各自动产出 1 条成片 → 统一存固定文件夹供运营
> 原则：**保留现有 8385 工作台手动出片方式不动**，本系统是其上叠加的"周更流水线"，复用同一套后端（HTTP API / 共用函数），不重复造轮子。

---

## 0. 关键边界（先看，避免误解）

| 环节 | 本系统做到哪 | 不做哪 |
|---|---|---|
| 选题 | ✅ 每周一自动生成 15 条 | — |
| 二创改编 | ✅ 自动出三段式/双声稿 + 过违禁词 | — |
| 成片输出 | ✅ 自动渲染 mp4 到固定文件夹 | — |
| **发布** | ⚠️ **产出成品到固定文件夹，即视为"已备发"** | ❌ 不直接调抖音/视频号 API 上传（此前已定：平台 API 不接，防封号风险）；真发布由你在固定文件夹取片后手动上传，或用平台自带"定时发布" |

> 即：本系统的"定时发布"= **定时把成片生成并放好**。平台上传那一步保持人工（符合你"先审查再确认"的习惯，也避开平台接口红线）。

---

## 1. 总体流程

```
┌─────────────────┐   周日23:30    ┌──────────────────────────────┐
│ 定时触发         │──────────────▶│ M1 选题生成（LLM×账号定位）    │
│ (Task Scheduler) │               │  → 15 条选题 → topics/ + 队列  │
└─────────────────┘               └──────────────┬───────────────┘
                                                  │ 周一
┌─────────────────┐   周一08:00    ┌──────────────▼───────────────┐
│ 人工确认(可选)    │──┐            │ M2 选题筛选：人工勾14 或       │
│ studio 打开看15条 │  │            │   自动取相关度Top14（留1备份） │
└─────────────────┘  │            └──────────────┬───────────────┘
                     └─兜底──────▶                │
                                          ┌──────▼───────┐
                                          │ M3 内容二创   │ 每条：二创→违禁词→脚本
                                          └──────┬───────┘
┌─────────────────┐  每天15:00/20:00 ┌──────────▼───────────┐
│ 定时触发         │────────────────▶│ M4 成片输出           │
│ (Task Scheduler) │                 │  拉队列下2条→TTS→     │
└─────────────────┘                 │  滚动卡/数字人→mp4    │
                                     └──────────┬───────────┘
                                          ┌──────▼───────┐
                                          │ M5 文件管理   │
                                          │  → videos/成品 │
                                          │  → pkg/文案    │
                                          └───────────────┘
```

---

## 2. 账号定位配置（驱动选题与二创）

新建 `gpt_sovits/account_profile.json`（一次配置，长期复用）：

```json
{
  "account": "老张讲财税",
  "persona": "张德富，苏州，20+年财税实战，税务风险咨询",
  "audience": "B端老板 / 中小微企业主",
  "tone": "实战派、口语化、去AI痕迹、站在老板视角讲风险",
  "hit_topics": ["公转私","虚开发票","个人卡流水","暂估成本","股东借款","金税四期","社保入税","异常凭证"],
  "forbidden": "gpt_sovits/forbidden_words.py 红线（自动过筛）",
  "style": "三段式(开头钩子/正文/结尾留资) 或 男女双声对话",
  "weekly_count": 15,
  "publish_slots": ["15:00","20:00"]
}
```

---

## 3. 固定文件夹结构（成品统一存放）

```
gpt_sovits/output/weekly/
└── 2026-W31/                  ← 每周一个目录（ISO 周号）
    ├── README.md              ← 本周清单（选题/状态/成片路径）
    ├── queue.json             ← 生产队列（选题→二创→渲染 状态机）
    ├── topics/                ← M1 产出：15条选题 + 评分
    │   └── week_topics.md
    ├── scripts/               ← M3 产出：每条口播稿(md+json)
    │   ├── 01_公转私借款风险.md
    │   └── ...
    ├── audio/                 ← TTS 中间产物
    ├── videos/                ← M4 成品（运营取片处）
    │   ├── W31-01-公转私借款风险.mp4
    │   ├── W31-01-公转私借款风险.txt   ← 口播稿+发布文案 sidecar
    │   └── ...
    └── pkg/                   ← 字幕/发布文案等附属
```

> 文件名规则：`<周号>-<序号>-<选题slug>.<ext>`，序号即发布顺序（01=周一15:00，02=周一20:00 … 14=周日20:00）。
> 旧周目录保留最近 4 周，更早的自动移入 `output/weekly/_archive/`。

---

## 4. 模块设计

### M1 选题生成（周日 23:30）
- **做法**：读 `account_profile.json` → 调 `rewrite_studio.py` 里已有的 `search_and_create(mode="list")` 思路（Tavily 联网检索 + DeepSeek 提炼），或直接用 `model_providers.get_text_config()` 拿 DeepSeek，套"爆款选题"提示词生成 **15 条**。
- **每条结构**：`标题 / 角度 / 老板痛点 / 为什么爆(钩子) / 适合平台 / 预估时长 / 相关度(0-100)`。
- **落地文件**：`gpt_sovits/weekly_pipeline.py gen-topics --week 2026-W31`
  - 内部：import `model_providers` 拿 key → 调 LLM → 写 `topics/week_topics.md` + 初始化 `queue.json`（15 条 status=`pending`）。
- **复用**：与 studio `/api/topic_search` 同源逻辑，不重写。

### M2 选题筛选（周一，人工+兜底）
- **人工路径（推荐）**：周一打开 8385 → 看 `topics/week_topics.md` → 在 `queue.json` 里把要做的 14 条标 `selected=true`（留 1 条备份）。符合你"先审查再确认"。
- **自动兜底**：若周一 08:00 前无人确认，自动化把相关度 Top14 自动标 `selected`（保证流水线不空转）。
- 落点：同一个 `queue.json`。

### M3 内容二创（渲染前即时，或提前批量）
- **做法**：对每条 `selected` 选题，调 `search_and_create(mode="create")` / `generate_from_source` 思路 → 产出：
  - 单声独白三段式（`ta-open/body/end`）
  - 或男女双声对话稿（`dialogue`，用于滚动字幕卡）
- 自动跑 `forbidden_words.scan()` 过红线，命中高危则退回 LLM 换表述（复用现有改写链路）。
- **落地**：`weekly_pipeline.py create-scripts --week ...` → 写 `scripts/*.md` + `*.json`（含 dialogue 字段）。

### M4 成片输出（每天 15:00 / 20:00）
- **拉队列**：取 `queue.json` 中下一个未完成 `selected` 选题（按序号）。
- **渲染**：复用现有两种出片方式——
  - **双声稿 → 滚动字幕卡**：`make_scroll_video.py --dialogue d.txt --out videos/Wxx-NN-x.mp4 [--title --subtitle --bg]`（无需 Docker，最快）
  - **单声独白 → 本地数字人**：`make_avatar_video.py`（HEYGEM，需 Docker 起；py310 跑）
  - 选哪种由选题 `style` 字段决定（默认交替或按账号配比）。
- **TTS**：直接复用 `make_scroll_video.synth_dialogue_audio` / `rewrite_studio.do_tts_dialogue`，音色 zhangc2 / jiangnv3 不变。
- **sidecar**：同目录生成 `<同名>.txt`（口播稿 + 发布文案 + 话题标签），方便你直接复制发布。
- **落地**：`weekly_pipeline.py render --week ... --slot afternoon|evening`
  - 内部：import `make_scroll_video` / 调 `make_avatar_video` 子进程 → 写 `videos/` → 更新 `queue.json` status=`done`。

### M5 文件管理
- `weekly_pipeline.py` 统一管目录创建、命名、README 清单、旧周归档。
- 每次渲染完刷新 `README.md`（哪条完成、路径、时长）。

### M6 定时调度（Task Scheduler，镜像现有 HGTStudioAutoPush 模式）
新增 4 个 Windows 计划任务（用系统 `schtasks`，与现有 03:00 git push 同机制）：

| 任务名 | 触发 | 动作 |
|---|---|---|
| `HGTWeeklyTopics` | 每周日 23:30 | `python weekly_pipeline.py gen-topics`（算当周 ISO 周号） |
| `HGTWeeklySelect` | 每周一 08:00 | `weekly_pipeline.py select --mode auto`（仅当未人工确认时生效） |
| `HGTRenderPM` | 每天 15:00 | `weekly_pipeline.py render --slot afternoon` |
| `HGTRenderEVE` | 每天 20:00 | `weekly_pipeline.py render --slot evening` |

> 注：渲染依赖 8385 服务(NSSM 常驻) 与 Docker(数字人)。服务崩了会自起；Docker 需开机自启（已在环境铁律中）。`weekly_pipeline.py` 渲染前会 `health_check` 8385，不通则写告警到 `queue.json` 并跳过本次，下个时段重试。

---

## 5. 与现有系统的关系（不破坏）

- **8385 工作台 UI**：完全保留，手动/按需出片照常。自动化流水线是从命令行调同一后端，互不干扰。
- **现有脚本**：`rewrite_studio.py` / `make_scroll_video.py` / `make_avatar_video.py` / `forbidden_words.py` / `model_providers.py` 全部**原地复用**，不改动其出片逻辑，只在 `weekly_pipeline.py` 里 import / 调 HTTP API。
- **音色 / 模特 / 违禁词库**：沿用既有配置，零改动。
- **新增文件**：仅 `account_profile.json` + `weekly_pipeline.py` + `output/weekly/` 目录 + 4 个计划任务。

---

## 6. 待确认决策点（开工前请拍板）

1. **选题确认方式**：人工周一勾 14 条（推荐，符合"先审查再确认"） vs 纯自动 Top14？→ 建议**人工+08:00自动兜底**。
2. **成片形态配比**：下午/晚上各用哪种？建议 **下午=本地数字人独白、晚上=双声滚动卡**（错峰观感），或按选题 `style` 自动。请选。
3. **发布边界**：维持"生成到固定文件夹=备发"（推荐，不碰平台API）还是想重新评估接平台定时发布 API？→ 此前已砍，默认维持。
4. **选题数量 vs 发布量**：15 选 14（留 1 备份），还是直接生成 14 条不要备份？

---

## 7. 落地步骤（确认后分模块交付）

1. 写 `account_profile.json`（账号定位，一次）
2. 写 `weekly_pipeline.py`：`gen-topics` → `select` → `create-scripts` → `render` → `archive`，含 health_check + 重试
3. 加 4 个 Task Scheduler 任务（脚本验证后可批量建）
4. 跑一次 dry-run：手动 `gen-topics` + `render` 一条，验证成品进 `videos/`
5. 观察一周，调配比/时段，再常态化

---

## 附：核心复用清单（证明不重复造轮子）
- 选题：`rewrite_studio.search_and_create` / `/api/topic_search` 同源
- 二创：`search_and_create(mode=create)` / `generate_from_source` + `forbidden_words.scan`
- TTS：`make_scroll_video.synth_dialogue_audio` / `rewrite_studio.do_tts_dialogue`（zhangc2 / jiangnv3）
- 出片：`make_scroll_video.py`（滚动卡）/ `make_avatar_video.py`（数字人）
- Key：`model_providers.get_text_config()` / `get_tts_config()`
- 调度范式：复用现有 `HGTStudioAutoPush`（Task Scheduler + 系统 python）

---

## 实施状态（2026-07-28 已落地）
- ✅ 已新增 `account_profile.json` + `weekly_pipeline.py`（gen-topics/select/create-scripts/render/archive/health）。
- ✅ 已建 4 个自动化任务并 ACTIVE：选题生成(周日23:30)、二创准备(周一08:00)、成片下午(15:00)、成片晚上(20:00)。
- ✅ dry-run 全链路通过；真实配音样片渲染验证中。
- 现有 8385 工作台 / 数字人出片完全保留，自动化是其上叠加的命令行流水线。
- 说明：调度采用产品内 automation 框架（避开 Windows 管理员权限）；如需 OS 级计划任务，可改 `schtasks` 镜像 `HGTStudioAutoPush`。
