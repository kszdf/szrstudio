# 2026-08-04 工作日志

## 周更短视频流水线（晚上 20:00 自动化）
- 运行 `weekly_pipeline.py render --slot evening`（python 3.13.12），渲染 2026-W32 首个晚间待渲染选题 **seq2 虚开发票罪量刑标准变了**。
- 产物：`output/weekly/2026-W32/videos/2026-W32-02-虚开发票罪量刑标准变了.mp4`（~32MB，1080×1920 滚动字幕卡点视频，走 make_scroll_video.py，非 HEYGEM 数字人管线）+ 同名 .txt sidecar（发布文案+口播稿三段）。
- queue.json seq2→done、video_path 已填；README.md 已刷新。耗时约 3m27s，无异常。
- 注意：本机 `tasklist //FI "IMAGENAME eq python.exe"` 在 Git Bash 下过滤语法会漏掉进程，应改用 `tasklist | grep -i python` 核查；render 进程输出经管道 block-buffered，日志即时为空属正常，直接看 videos/ 目录产出即可。
